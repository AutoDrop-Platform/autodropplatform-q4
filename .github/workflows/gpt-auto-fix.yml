name: AutoGPT-Coder

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  code-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install OpenAI CLI
        run: |
          npm install -g openai

      - name: Run GPT Code Assistant
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          npx openai api chat.completions.create \
            -m gpt-4 \
            -t "راجع كود المشروع وعدل المشاكل إن وجدت" \
            -i ./src \
            --temperature 0.3

      - name: Commit changes
        run: |
          git config --global user.email "autobot@autodropplatform.shop"
          git config --global user.name "Smart Auto Bot"
          git add .
          git commit -m "🔧 إصلاحات تلقائية من GPT-4" || echo "No changes"
          git push || echo "No push"
